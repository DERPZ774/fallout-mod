package com.derpz.nukaisl.util;

import com.derpz.nukaisl.FalloutMod;
import com.derpz.nukaisl.item.ModItems;
import net.minecraft.client.renderer.item.ItemProperties;
import net.minecraft.resources.ResourceLocation;

public class ModItemProperties {
    //method for changing textures via [data]
    /*public static void addCustomItemProperties() {
        ItemProperties.register(ModItems.DEBUG_TABLET_ITEM.get(), new ResourceLocation(FalloutMod.MOD_ID, "on"),
                ((pStack, pLevel, pEntity, pSeed) -> pStack.hasTag() ? 1f : 0f));

        ItemProperties.register(ModItems.NUKA_COLA.get(), new ResourceLocation(FalloutMod.MOD_ID, "cap"),
                ((pStack, pLevel, pEntity, pSeed) -> pStack.hasTag() ? 1f : 0f));


    }

     */
}
